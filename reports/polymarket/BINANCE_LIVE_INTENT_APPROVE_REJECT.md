# Binance Live Intent Approve / Reject

Added:
- `POST /api/nexora/binance/live/intent/:id/approve`
- `POST /api/nexora/binance/live/intent/:id/reject`

This creates the owner approval workflow before any supervised execution endpoint is added.
